#! /bin/bash

SRC=libtool-2.4.6.tar.xz       
DIR=libtool-2.4.6

cd /sources

if [ -d $DIR ]; then
  rm -rf $DIR
fi

tar -xf $SRC
cd $DIR

./configure --prefix=/usr > /dev/null
make -j8 -s > /dev/null
make -j8 check
make install > /dev/null

rm -fv /usr/lib/libltdl.a
